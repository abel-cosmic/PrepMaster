export default function DisplayScore({ score, ExamTitle, CourseTitle }) {
  return (
    <div>
      <p>Hello</p>
      <h1>{score}</h1>
      <h1>{ExamTitle}</h1>
      <h1>{CourseTitle}</h1>
    </div>
  );
}
